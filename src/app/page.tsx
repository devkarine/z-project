import { Logo } from "@/components/ui/logo";

const Page = () => {
  return (
    <div className="min-h-screen flex justify-center items-center">
      <Logo size={80} />
    </div>
  );
};

export default Page;
